import React from 'react';
import ReactDOM from 'react-dom/client';

function Example() {
    return <h2>React component inside Laravel + Livewire project</h2>;
}

ReactDOM.createRoot(document.getElementById('react-root')).render(<Example />);
